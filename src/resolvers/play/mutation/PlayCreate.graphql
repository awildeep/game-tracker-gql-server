mutation playCreate($playCreateRequest: PlayCreateInput!) {
  PlayCreate(playCreateRequest: $playCreateRequest){
    play_id
    week {
        week_id
        date_played
    }
    player {
        player_id
        name
        active
    }
    rank
  }
}