query play($playRequest: PlayInput!) {
  Play(playRequest: $playRequest){
    play_id
    week {
        week_id
        date_played
    }
    player {
        player_id
        name
        active
    }
    rank
  }
}