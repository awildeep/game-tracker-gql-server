query playsByWeek($playByWeekRequest: PlaysByWeekInput!) {
  PlaysByWeek(playsByWeekRequest: $playByWeekRequest){
    play_id
    week {
        week_id
        date_played
    }
    player {
        player_id
        name
        active
    }
    rank
  }
}